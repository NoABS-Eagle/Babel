__pycache__/
*.pyc

# Personal Claude Code permission overrides, not shared config
.claude/settings.local.json

# Eagle editor auto-backup files (.l#1, .l#2, .s#1, ...)
*.l#*
*.s#*
*.b#*

# KiCad local-history plugin artifacts (embedded git repos, break `git add`)
.history/

# KiCad lock files
~*.lck
*.lck

# Test fixtures: real projects from various EDA tools, used to run tests
# against, not source (project decision, 2026-09-04)
testData/

# Converter run results — regenerated, not source
output/
